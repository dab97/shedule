# План на следующую сессию: Виртуализация (Infinite Scroll)

Мы полностью закончили с визуалом (иконки, баннеры, метатеги) и зарелизили версию **0.1.5**.
Следующая задача — переписать таблицу расписания на виртуальный список для производительности.

## 🏁 С чего начать завтра:

1.  **Установка зависимостей**:
    ```bash
    npm install @tanstack/react-virtual
    ```

2.  **Создание компонента**:
    - Создать `components/VirtualScheduleTable.tsx`.
    - Перенести логику из `ScheduleTable.tsx`, заменив `<table>` на `div` (flex/grid).

3.  **Логика**:
    - Подключить хук `useVirtualizer`.
    - Настроить динамическую высоту строк (так как пары могут иметь разное кол-во контента).

## Структура файлов

### [NEW] components/VirtualScheduleTable.tsx
Будет содержать новую реализацию таблицы.

### [MODIFY] components/schedule-view.tsx
Заменим старый `<ScheduleTable />` на новый `<VirtualScheduleTable />`.

---

**Текущий статус:**
- [x] Брендинг и Иконики (v0.1.5)
- [ ] Виртуализация (В ожидании старта)
