# Руководство по Деплою на Cloudflare Pages

Это руководство поможет вам развернуть приложение расписания РГСУ на Cloudflare Pages.

## Предварительные Требования

- ✅ Аккаунт Cloudflare (бесплатный план подходит)
- ✅ Git репозиторий на GitHub или GitLab
- ✅ Все изменения закоммичены и запушены в репозиторий

## Шаг 1: Подготовка Проекта

### 1.1 Обновление Зависимостей

Установите обновленные зависимости (Vercel-пакеты уже удалены):

```bash
npm install
```

### 1.2 Проверка Локальной Сборки

Убедитесь, что проект собирается без ошибок:

```bash
npm run build
```

## Шаг 2: Создание Проекта на Cloudflare Pages

### 2.1 Вход в Cloudflare Dashboard

1. Перейдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Войдите в свой аккаунт или создайте новый (бесплатно)

### 2.2 Создание Pages Проекта

1. В боковом меню выберите **Workers & Pages**
2. Нажмите **Create application**
3. Выберите вкладку **Pages**
4. Нажмите **Connect to Git**

### 2.3 Подключение Git Репозитория

1. Выберите **GitHub** или **GitLab**
2. Авторизуйте Cloudflare для доступа к вашим репозиториям
3. Выберите репозиторий с проектом `shedule`
4. Нажмите **Begin setup**

## Шаг 3: Настройка Сборки

### 3.1 Основные Настройки

Заполните следующие поля:

- **Project name**: `rgsu-schedule` (или любое другое имя)
- **Production branch**: `main` (или ваша основная ветка)
- **Framework preset**: `Next.js`

### 3.2 Build Settings

Cloudflare автоматически определит настройки для Next.js, но проверьте:

- **Build command**: `npm run build`
- **Build output directory**: `.vercel/output/static`

> **Важно**: Next.js на Cloudflare Pages использует статический экспорт. Некоторые серверные функции могут работать иначе.

## Шаг 4: Настройка Переменных Окружения

### 4.1 Добавление Environment Variables

1. Прокрутите вниз до секции **Environment variables**
2. Нажмите **Add variable**
3. Добавьте все переменные из вашего `.env` файла:

**Обязательные переменные** (примеры, используйте свои значения):

```
NEXT_PUBLIC_SITE_URL=https://rgsu-schedule.pages.dev
UPSTASH_REDIS_REST_URL=your_upstash_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_token
GOOGLE_CLIENT_EMAIL=your_service_account_email
GOOGLE_PRIVATE_KEY=your_private_key
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
GEMINI_API_KEY=your_gemini_api_key
```

> **Важно**: Для многострочных значений (например, `GOOGLE_PRIVATE_KEY`) используйте кнопку "Edit as text" и вставьте значение с переносами строк.

### 4.2 Обновление URL

После первого деплоя Cloudflare даст вам URL вида `https://rgsu-schedule.pages.dev`. Обновите переменную:

```
NEXT_PUBLIC_SITE_URL=https://ваш-проект.pages.dev
```

## Шаг 5: Деплой

1. Нажмите **Save and Deploy**
2. Cloudflare начнет сборку проекта (обычно 2-5 минут)
3. Следите за прогрессом в логах сборки

### 5.1 Проверка Деплоя

После успешной сборки:

1. Перейдите по предоставленному URL
2. Проверьте основные функции:
   - ✅ Загрузка расписания
   - ✅ Выбор даты
   - ✅ Экспорт в PDF
   - ✅ Переключение темы
   - ✅ PWA установка

## Шаг 6: Настройка Cron Job (Важно!)

Cloudflare Pages не поддерживает cron jobs напрямую. У вас есть два варианта:

### Вариант 1: Cloudflare Workers Cron Triggers (Рекомендуется)

1. В Cloudflare Dashboard перейдите в **Workers & Pages**
2. Создайте новый Worker
3. Используйте следующий код:

```javascript
export default {
  async scheduled(event, env, ctx) {
    await fetch('https://ваш-проект.pages.dev/api/push/check', {
      method: 'GET',
    });
  },
};
```

4. Настройте Cron Trigger: `0 8 * * *` (каждый день в 8:00)

### Вариант 2: Внешний Сервис

Используйте бесплатный сервис [cron-job.org](https://cron-job.org):

1. Зарегистрируйтесь на cron-job.org
2. Создайте новый cron job
3. URL: `https://ваш-проект.pages.dev/api/push/check`
4. Расписание: `0 8 * * *`

## Шаг 7: Настройка Аналитики (Опционально)

### Cloudflare Web Analytics

1. В Cloudflare Dashboard перейдите в **Analytics & Logs** → **Web Analytics**
2. Нажмите **Add a site**
3. Введите URL вашего сайта
4. Скопируйте предоставленный JavaScript код
5. Добавьте его в `app/layout.tsx` внутри `<body>`:

```typescript
<Script
  defer
  src='https://static.cloudflareinsights.com/beacon.min.js'
  data-cf-beacon='{"token": "ваш-токен"}'
/>
```

## Шаг 8: Настройка Кастомного Домена (Опционально)

Если у вас есть свой домен:

1. В настройках проекта перейдите в **Custom domains**
2. Нажмите **Set up a custom domain**
3. Введите ваш домен
4. Следуйте инструкциям по настройке DNS

## Автоматические Деплои

Cloudflare Pages автоматически деплоит изменения при каждом push в основную ветку:

- **Production**: деплой из `main` ветки
- **Preview**: деплой из других веток (для тестирования)

## Troubleshooting

### Ошибка сборки: "Module not found"

Убедитесь, что все зависимости установлены:
```bash
npm install
```

### Ошибка: "Environment variable not found"

Проверьте, что все переменные окружения добавлены в настройках Cloudflare Pages.

### API Routes не работают

Next.js на Cloudflare Pages использует Edge Runtime. Убедитесь, что ваши API routes совместимы с Edge Runtime.

### Проблемы с PWA

Проверьте, что файлы `manifest.json` и service worker корректно генерируются в `.vercel/output/static/`.

## Мониторинг и Логи

- **Build logs**: доступны в разделе **Deployments**
- **Function logs**: доступны в разделе **Functions**
- **Analytics**: доступны в разделе **Analytics**

## Лимиты Бесплатного Плана

- ✅ **Bandwidth**: Неограничен
- ✅ **Builds**: 500/месяц
- ✅ **Requests**: 100,000/день
- ✅ **Build time**: 20 минут на сборку

## Полезные Ссылки

- [Cloudflare Pages Docs](https://developers.cloudflare.com/pages/)
- [Next.js on Cloudflare Pages](https://developers.cloudflare.com/pages/framework-guides/nextjs/)
- [Cloudflare Workers Cron Triggers](https://developers.cloudflare.com/workers/configuration/cron-triggers/)

---

## Следующие Шаги

После успешного деплоя:

1. ✅ Обновите переменную `NEXT_PUBLIC_SITE_URL` на актуальный URL
2. ✅ Настройте cron job для `/api/push/check`
3. ✅ (Опционально) Настройте Cloudflare Web Analytics
4. ✅ (Опционально) Подключите кастомный домен
5. ✅ Удалите старый проект с Vercel (если больше не нужен)

**Поздравляем! Ваше приложение теперь работает на Cloudflare Pages! 🎉**
